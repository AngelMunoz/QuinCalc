<Page
    x:Class="QuinCalc.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:QuinCalc"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  xmlns:model="using:QuinCalc.ViewModels"
  mc:Ignorable="d">

  <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <CommandBar VerticalAlignment="Bottom">
      <AppBarSeparator/>
      <AppBarToggleButton Icon="Calculator" Label="Expenses"/>
      <AppBarToggleButton Icon="List" Label="Todo's"/>

      <CommandBar.SecondaryCommands>
        <AppBarButton Icon="Add" Label="Add Expense" Click="AddExpense_Click"/>
        <AppBarButton Icon="Add" Label="Add Todo" Click="AddTodo_Click"/>
      </CommandBar.SecondaryCommands>

      <CommandBar.Content>
        <StackPanel Width="auto" Orientation="Horizontal">
          <StackPanel Padding="0,0,5,0" Orientation="Vertical">
            <TextBlock>Up Next: </TextBlock>
            <TextBlock Text="{x:Bind UpNext.Description}"/>
          </StackPanel>
          <StackPanel Padding="0,0,5,0" Orientation="Vertical">
            <TextBlock>Due to:</TextBlock>
            <TextBlock Text="{x:Bind UpNext.DueDate.ToLongDateString()}"/>
          </StackPanel>
        </StackPanel>
      </CommandBar.Content>
    </CommandBar>
    <Pivot Title="QuinCalc" Margin="0,0,0,54">
      <PivotItem Header="Expenses" Margin="12,0,12,46.8">
        <StackPanel HorizontalAlignment="Left" Width="auto">
          <ListView ItemsSource="{x:Bind Expenses}">
            <ListViewHeaderItem>
              This are the foreseen Expenses...
            </ListViewHeaderItem>
            <ListView.ItemTemplate>
              <DataTemplate x:DataType="model:ExpenseViewModel">
                <StackPanel Orientation="Vertical" Height="50">
                  <TextBlock Text="{x:Bind Name}"/>
                  <TextBlock Text="{x:Bind Amount}"/>
                </StackPanel>
              </DataTemplate>
            </ListView.ItemTemplate>
          </ListView>
        </StackPanel>
      </PivotItem>
      <PivotItem Header="Todos" Margin="12,0,12,49.8">
        <StackPanel HorizontalAlignment="Left" Width="auto">
          <ListView ItemsSource="{x:Bind Todos}">
            <ListViewHeaderItem>
              This are the current Todos...
            </ListViewHeaderItem>
            <ListView.ItemTemplate>
              <DataTemplate x:DataType="model:TodoViewModel">
                <StackPanel Orientation="Vertical" Height="50">
                  <TextBlock Text="{x:Bind Name}"/>
                  <TextBlock Text="{x:Bind Description}"/>
                </StackPanel>
              </DataTemplate>
            </ListView.ItemTemplate>
          </ListView>
        </StackPanel>
      </PivotItem>
    </Pivot>
  </Grid>
</Page>
